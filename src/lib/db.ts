/** @deprecated Use @/lib/types and @/lib/memory — kept for import compatibility */
export type {
  JournalEntry,
  EmotionalMoment,
  WorryThread,
  BrightThread,
  MindThread,
} from "@/lib/types";
